;;; bpr-config.el --- Provides and configures bpr
;;; Commentary:
;;
;; bpr gives us a way to asynchronously run things in the background.
;; Background Process Runner.
;;
;;; Code:

(use-package bpr)

(provide 'bpr-config)
;;; bpr-config ends here
